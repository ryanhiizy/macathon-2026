# Profile

- The profile tab reads the signed-in user's `profiles` row from Supabase.
- The edit profile modal updates `display_name`, `handle`, and `bio`.
- Avatar upload is not implemented for the hackathon build, but the modal shows the current avatar.
- Saved edits refresh on return to the profile tab so the demo path feels immediate.
