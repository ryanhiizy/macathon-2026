[project]
name = "presence-backend"
version = "0.1.0"
description = "Local prompt and verification server for presence"
requires-python = ">=3.10"
dependencies = [
  "anthropic",
  "fastapi",
  "openai",
  "python-dotenv",
  "python-multipart",
  "uvicorn[standard]",
]

[tool.uv]
package = false
